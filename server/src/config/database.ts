export const CONFIG_DB = {
    options: {
        autoReconnect: true,
        reconnectInterval: 1000,
        reconnectTries: Number.MAX_VALUE,
    },
    url: "mongodb://localhost:27017/semeat",
};
